#Development Section
* Run a NestJS server: `npm run start:dev`
* Run in `client/` `ng build --watch` when files change


# Testing Section
* Run test for NestJS: `npm run test`

* Run test for NestJS: `ng test`